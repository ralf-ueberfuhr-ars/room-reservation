apply plugin: 'java'

jar {
    archiveBaseName  = 'domain'
}

tasks.register('packageLayerLib', Zip) {
    archiveFileName = 'domain-layer.zip'
    into('java/lib') {
        from(jar)
    }
}

build.dependsOn packageLayerLib